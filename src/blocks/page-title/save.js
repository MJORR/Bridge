/**
 * Save callback for `bridge/page-title`.
 *
 * Fully dynamic block — render.php produces all frontend HTML and reads the
 * per-page banner settings from post meta. Nothing is serialised between the
 * block comments.
 */
const Save = () => null;

export default Save;
